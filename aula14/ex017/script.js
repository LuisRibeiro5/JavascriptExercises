function calculo() {
    var num =
    var mult =
    var resul =

    if(num == 0 || mult == 0){
        window.alert('Digite em todos os espaços')
    } else {
    
        
    for (c = 1; c <= 10; c++) {
        var resul = num * c 
        res.innerHTML = `${resul}`
    }
   }
}
